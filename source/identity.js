/**
 * Returns the same value.
 *
 * @template {any} Parameter
 *
 * @param {Parameter} value
 *
 * @returns {Parameter}
 *
 * @example
 * ```javascript
 * identity(42) // => 42
 * ```
 */
export var identity = (value) => value
